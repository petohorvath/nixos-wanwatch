/*
  types/probe — NixOS option types for the Probe value type.

  Pass 1 boundary: empty stub. Pass 5 will export `probe`,
  `probeMethod`, `probeTarget`, `probeThresholds`, `probeHysteresis`,
  `probeFamilyHealthPolicy`, and any related primitives — flattened
  into `wanwatch.types.<name>` by `lib/types/default.nix`.
*/
_: { }

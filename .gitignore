# nix build output symlinks
result
result-*

# git-hooks.nix regenerates .pre-commit-config.yaml on every
# `nix develop`; the source of truth is `preCommitCheckFor` in
# flake.nix.
.pre-commit-config.yaml

# direnv
.direnv/

# go build output
/daemon/wanwatchd
/daemon/dist/

# go test artifacts
*.test
*.out
coverage.txt

# editor autosave / backup files
*~
\#*\#
.\#*
.idea/
.vscode/
